# Auto detect text files and perform LF normalization
* text=auto

# Binary test fixtures — prevent line-ending normalization and end-of-file-fixer
# from corrupting these files during pre-commit hook runs.
*.db binary
*.rrd binary
*.png binary
